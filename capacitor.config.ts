import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'yup-input-validation',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
